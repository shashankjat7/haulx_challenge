import 'package:flutter/material.dart';

class PastLoadsPage extends StatelessWidget {
  const PastLoadsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'You were not supposed to look here!',
        textAlign: TextAlign.center,
      ),
    );
  }
}
